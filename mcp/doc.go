// Package mcp exposes the core engine over the Model Context Protocol, so
// Claude can query a vault. It is registered per-vault via a project-scoped
// .mcp.json. Built on the Go MCP SDK.
package mcp

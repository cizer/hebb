// Package core is the UI-agnostic hebb engine: indexing, search, vault
// scaffolding, sync and hygiene. The CLI and the MCP surface are thin layers
// over this package, so a future GUI can reuse it without rework.
package core

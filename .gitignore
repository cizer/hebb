# Go build output
/hebb
/dist/
*.test
*.out

# Per-vault derived state (lives in each vault, never committed here)
.hebb/index.db
.hebb/index.db-*

# Local Claude Code session state
.claude/settings.local.json

# OS
.DS_Store

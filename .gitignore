# Go build output
/hebb
/dist/
*.test
*.out

# Per-vault derived state (lives in each vault, never committed here)
.hebb/index.db
.hebb/index.db-*

# Local Claude Code session state
.claude/settings.local.json

# OS
.DS_Store

# Throwaway plugin spike (not part of the build)
/spike/

# internal dev tracker, not for the public repo
/PLAN.md
